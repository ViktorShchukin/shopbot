### Current task list:
1. Docker has his own logs on server. Log rotation isn't set.  
2. Need to implement retry logic for telegram client methods. Maybe by myself, maybe serch in internet.
3. clean repository from shop part.
4. Add chat memeger update type.
5. can get chatId from callback query. Maybe i didn't do this because i thought that i shouldn't include telegram logic into domain. But it cost me additional request in database. 
6. A lot of people don't spend even a minute in the bot. maybe create logging for events. like get telegram update. do this and that. send message and recive a respone. 
7. have idea to create front end for log. don't want to ssh to server every time.
8. page to check users. maybe some statistic and charts.